# Datatypes and Variables
# Input and If - Else statements
# Function = input()
# We use the input() to receive user input
first_name = input("Enter the first name: ") #"John"
last_name = input("Enter the last name: ") #"Doe"
age = input("Enter the age: ") #41

# input() - function to receive input from the user #Hit Enter
# TypeError = I can't add a string to an integer
# Add a string to a integer
print(first_name+" "+last_name+" "+"is"+" "+age+" "+"years old")